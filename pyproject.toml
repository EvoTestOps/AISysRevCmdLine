[project]
name = "aisysrevcmdline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.13.3",
    "asyncio>=4.0.0",
    "pandas>=2.3.3",
    "plotly>=6.5.1",
    "pydantic>=2.12.5",
    "pydantic-ai>=1.41.0",
    "pydantic-ai-slim[retries]>=1.41.0",
    "pytest>=9.0.2",
    "tqdm>=4.67.1",
    "krippendorff>=0.6.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
markers = [
    "slow: marks tests as slow (run with -m slow; requires generated fixture files)",
]
